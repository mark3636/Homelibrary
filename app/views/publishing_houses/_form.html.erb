<%= form_with(model: publishing_house, local: true) do |form| %>
  <% if publishing_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(publishing_house.errors.count, "error") %> prohibited this publishing_house from being saved:</h2>

      <ul>
      <% publishing_house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :publisihng_house_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
