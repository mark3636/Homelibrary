%td{colspan: 2}
  = form_for object, url: object.new_record? ? publishing_houses_path : publishing_house_path(object),
                     remote: true, html: {class: "form-horizontal"} do |f|
    %p
      Name:
      = f.text_field :name

    = f.submit "Save", class: "btn btn-primary"
    %button.btn.btn-link.cancel Cancel
